const VIDEO_SPECS = {
    height: document.documentElement.clientHeight * 0.40,
    width: document.documentElement.clientWidth * 0.36,
    freq: 0.5,
    face_thresh: 0.85,
    hand_thresh: 0.90,
};

export default VIDEO_SPECS;